// Test nested loops with array indexing

func test_nested_loops_array_indexing(nums : []int) -> []int {
    for i := 0; i < len(nums); i = i + 1 {
        for j := i + 1; j < len(nums); j = j + 1 {
            first := nums[i];
            second := nums[j];
            return [first, second];
        }
    }
    return [-1, -1];
}

func main() {
    log("test");
} 